using UnityEngine;

public class ManagerKeep : MonoBehaviour
{
    private static ManagerKeep instance;

    private void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
