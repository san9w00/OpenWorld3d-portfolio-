using UnityEngine;

public class CameraKeep : MonoBehaviour
{
    private static CameraKeep instance;

    private void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
